#include <parallax/core/execution_context.hpp>

namespace parallax::core {

// ExecutionContext currently consists only of directly owned core resources.
// Resource initialization and deterministic teardown will be added here as
// accelerator and CPU execution lanes are introduced

} 